type User = {
    name: string;
    isActive: boolean;
};

const allUsers: User[] = [];

allUsers.push({
    name: 'true',
    isActive: false,
});

const MLModels: number[][] = [
    [255, 255, 255], 
    []
];

function doSomething(val: Array<string>) {
    // ...
}

const myArr: string[] = ["hello", "world"];

doSomething(myArr);

export {};
